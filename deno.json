{
  "nodeModulesDir": "auto",
  "tasks": {
    "check": {
      "description": "全チェック (フォーマット・リント・型・テスト) を一括実行",
      "command": "deno task check:fmt && deno task check:lint && deno task check:types && deno task test"
    },
    "check:fmt": "deno fmt --check src",
    "check:lint": "deno lint src",
    "check:types": "deno check src",
    "fix": {
      "description": "自動修正 (フォーマット・リント autofix) を一括実行",
      "command": "deno fmt src && deno lint --fix src"
    },
    "test": "deno test src",
    "build": "deno bundle --platform browser --minify --sourcemap=linked --outdir dist index.html",
    "serve": "deno task build && deno run --allow-net --allow-read=dist @std/http/file-server dist"
  },
  "fmt": {
    "lineWidth": 100
  },
  "imports": {
    "@std/assert": "jsr:@std/assert@^1.0.19",
    "@std/http": "jsr:@std/http@^1.1.1",
    "@types/react": "npm:@types/react@^19.2.17",
    "@types/react-dom": "npm:@types/react-dom@^19.2.3",
    "fast-check": "npm:fast-check@^4.8.0",
    "jotai": "npm:jotai@^2.20.0",
    "react": "npm:react@^19.2.7",
    "react-dom": "npm:react-dom@^19.2.7",
    "rxjs": "npm:rxjs@^7.8.2"
  }
}
